# src/

Empty on purpose. Lambda application source lands here in the Weeks 1–4
"Lambda + DynamoDB" build step, once the account structure in `terraform/`
(IAM role, VPC, KMS) is in place for it to deploy into.
